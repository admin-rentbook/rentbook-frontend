import { PropertyHeader } from './PropertyHeader';
import { PropertyList } from './PropertyList';

export const Property = () => {
  return (
    <div>
      <PropertyHeader />
      <PropertyList />
    </div>
  );
};
